const appConfig = {
  SOLANA_NETWORK: 'devnet',
  SOLANA_NETWORK_COMMITMENT: 'confirmed',
  LANDING_PAGE_SIGN_IN_ENABLED: true,
  LANDING_PAGE_SHOW_COMMUNITIES_SECTION: false,
  DEV_MODE_ENABLED: true,
};

export default appConfig;
